// Person - Name -- Age -- Married
let person:[string,number,boolean]=["Vignesh",30,true];
console.log(person);
let person2 = ["Vignesh",30,true];
let firstName = person[0];
person.push(false);
console.log(person);